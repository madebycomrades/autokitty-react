{
  "name":"autokitty-react",
  "description": "app.json configures the Heroku PR apps only",
  "scripts":{
    "postdeploy":"npm run jspm:install && npm run local:db:init && npm start"
  },
  "env":{
    "DB":".db/autokitty",
    "NODE_ENV":"production",
    "JSPM_GITHUB_AUTH_TOKEN": {
      "required": true
    }
  },
  "addons":[]
}
